# Rust 关键字与缩写速查表

> 📅 创建日期：2025-12-27
> 🎯 用途：快速查阅 Rust 缩写含义，对比 Java 理解

---

## 📝 关键字缩写对照

| Rust | 全称 | 中文 | Java 对应 | 示例 |
|------|------|------|-----------|------|
| `fn` | function | 函数 | `void/返回类型` | `fn main() {}` |
| `mod` | module | 模块 | `package` | `mod utils;` |
| `pub` | public | 公开 | `public` | `pub fn hello()` |
| `mut` | mutable | 可变 | - (默认可变) | `let mut x = 1;` |
| `impl` | implement | 实现 | `implements` | `impl User {}` |
| `struct` | structure | 结构体 | `class` | `struct User {}` |
| `enum` | enumeration | 枚举 | `enum` | `enum Color {}` |
| `ref` | reference | 引用 | - | `ref x = value;` |
| `dyn` | dynamic | 动态 | - | `Box<dyn Trait>` |
| `const` | constant | 常量 | `final` | `const MAX: i32 = 100;` |
| `static` | static | 静态 | `static` | `static NAME: &str = "test";` |
| `async` | asynchronous | 异步 | `async` (需要库) | `async fn fetch()` |
| `await` | await | 等待 | `await` | `future.await` |
| `use` | use | 引入 | `import` | `use std::io;` |
| `crate` | crate | 包/箱 | `module` | `crate::utils` |
| `self` | self | 自身 | `this` | `self.name` |
| `Self` | Self | 自身类型 | 类名 | `fn new() -> Self` |
| `let` | let | 绑定 | `var/类型` | `let x = 1;` |
| `if` | if | 如果 | `if` | `if x > 0 {}` |
| `else` | else | 否则 | `else` | `else {}` |
| `match` | match | 匹配 | `switch` | `match x {}` |
| `loop` | loop | 循环 | `while(true)` | `loop {}` |
| `for` | for | 遍历 | `for` | `for i in 0..10` |
| `while` | while | 当...时 | `while` | `while x > 0 {}` |
| `break` | break | 跳出 | `break` | `break;` |
| `continue` | continue | 继续 | `continue` | `continue;` |
| `return` | return | 返回 | `return` | `return x;` |
| `where` | where | 约束 | - | `where T: Clone` |
| `type` | type | 类型别名 | `typedef` (C) | `type Result<T> = ...` |
| `trait` | trait | 特征 | `interface` | `trait Printable {}` |
| `move` | move | 移动 | - | `move \|\| {}` |
| `unsafe` | unsafe | 不安全 | - | `unsafe {}` |
| `extern` | external | 外部 | `native` | `extern "C" {}` |

---

## 🔢 数据类型缩写

### 整数类型

| Rust | 全称 | 范围 | Java 对应 |
|------|------|------|-----------|
| `i8` | integer 8-bit | -128 ~ 127 | `byte` |
| `i16` | integer 16-bit | -32,768 ~ 32,767 | `short` |
| `i32` | integer 32-bit | -2³¹ ~ 2³¹-1 | `int` |
| `i64` | integer 64-bit | -2⁶³ ~ 2⁶³-1 | `long` |
| `i128` | integer 128-bit | 超大 | `BigInteger` |
| `isize` | integer size | 平台相关 | - |
| `u8` | unsigned 8-bit | 0 ~ 255 | - |
| `u16` | unsigned 16-bit | 0 ~ 65,535 | `char` (某种程度) |
| `u32` | unsigned 32-bit | 0 ~ 4,294,967,295 | - |
| `u64` | unsigned 64-bit | 0 ~ 2⁶⁴-1 | - |
| `u128` | unsigned 128-bit | 超大 | - |
| `usize` | unsigned size | 平台相关，用于索引 | - |

> 💡 `i` = integer（有符号），`u` = unsigned（无符号）
> 💡 数字 = 位数（bit）

### 浮点数类型

| Rust | 全称 | 精度 | Java 对应 |
|------|------|------|-----------|
| `f32` | float 32-bit | 单精度 | `float` |
| `f64` | float 64-bit | 双精度 | `double` |

### 其他常用类型

| Rust | 全称 | 说明 | Java 对应 |
|------|------|------|-----------|
| `bool` | boolean | 布尔值 | `boolean` |
| `char` | character | 字符（4字节Unicode） | `char` |
| `str` | string slice | 字符串切片（不可变） | - |
| `String` | String | 可变字符串 | `String` |
| `Vec<T>` | Vector | 动态数组 | `ArrayList<T>` |
| `Option<T>` | Option | 可选值 | `Optional<T>` |
| `Result<T,E>` | Result | 结果/错误 | `throws Exception` |
| `Box<T>` | Box | 堆上指针 | 自动装箱 |
| `Rc<T>` | Reference Counted | 引用计数 | - |
| `Arc<T>` | Atomic Rc | 原子引用计数（线程安全） | - |
| `&T` | reference | 不可变引用 | 引用 |
| `&mut T` | mutable reference | 可变引用 | 引用 |

---

## 🎯 常用符号速查

### 所有权与引用

| 符号 | 名称 | 含义 | 示例 |
|------|------|------|------|
| `&` | 引用 | 借用（不可变） | `&x` |
| `&mut` | 可变引用 | 借用（可变） | `&mut x` |
| `*` | 解引用 | 获取引用的值 | `*ptr` |
| `'a` | 生命周期 | 引用的有效范围 | `&'a str` |
| `'static` | 静态生命周期 | 整个程序期间有效 | `&'static str` |

### 模式匹配与解构

| 符号 | 名称 | 含义 | 示例 |
|------|------|------|------|
| `_` | 通配符 | 忽略值 | `let _ = x;` |
| `..` | 范围/剩余 | 剩余元素 | `..user` |
| `..=` | 包含范围 | 包含结束值 | `1..=5` |
| `@` | 绑定 | 绑定变量同时匹配 | `x @ 1..=5` |
| `\|` | 或 | 匹配多个模式 | `1 \| 2 \| 3` |

### 泛型与特征

| 符号 | 名称 | 含义 | 示例 |
|------|------|------|------|
| `<T>` | 泛型 | 泛型参数 | `Vec<T>` |
| `:` | 约束 | 特征约束 | `T: Clone` |
| `+` | 多约束 | 多个特征 | `T: Clone + Debug` |
| `impl Trait` | 实现特征 | 返回实现特征的类型 | `-> impl Display` |
| `dyn Trait` | 动态特征 | 特征对象 | `Box<dyn Display>` |

### 宏与属性

| 符号 | 名称 | 含义 | 示例 |
|------|------|------|------|
| `!` | 宏调用 | 调用宏 | `println!()` |
| `#[]` | 外部属性 | 应用于下一项 | `#[derive(Debug)]` |
| `#![]` | 内部属性 | 应用于当前项 | `#![allow(unused)]` |

### 其他常用符号

| 符号 | 名称 | 含义 | 示例 |
|------|------|------|------|
| `?` | 错误传播 | 提前返回错误 | `file.read()?` |
| `=>` | 箭头 | 匹配分支 | `1 => "one"` |
| `->` | 返回类型 | 函数返回类型 | `fn foo() -> i32` |
| `::` | 路径分隔符 | 访问模块/关联项 | `std::io::Read` |
| `\|\|` | 闭包 | 闭包参数 | `\|x\| x + 1` |

---

## 📦 常用宏速查

| 宏 | 用途 | 示例 |
|----|------|------|
| `println!` | 打印并换行 | `println!("Hello, {}", name);` |
| `print!` | 打印不换行 | `print!("Hi ");` |
| `format!` | 格式化字符串 | `let s = format!("{} {}", a, b);` |
| `dbg!` | 调试打印 | `dbg!(x);` |
| `panic!` | 恐慌退出 | `panic!("出错了！");` |
| `assert!` | 断言 | `assert!(x > 0);` |
| `assert_eq!` | 相等断言 | `assert_eq!(a, b);` |
| `vec!` | 创建 Vec | `let v = vec![1, 2, 3];` |
| `todo!` | 待实现 | `todo!("以后再写");` |
| `unimplemented!` | 未实现 | `unimplemented!();` |
| `include_str!` | 包含文件内容 | `include_str!("file.txt");` |
| `cfg!` | 条件编译检查 | `if cfg!(debug) {}` |

---

## 🔄 Rust vs Java 概念对照

| 概念 | Java | Rust |
|------|------|------|
| 类 | `class User {}` | `struct User {}` |
| 接口 | `interface Printable {}` | `trait Printable {}` |
| 实现接口 | `class X implements Y` | `impl Y for X {}` |
| 方法 | 类内定义 | `impl X { fn method() }` |
| 继承 | `extends` | 无继承，用组合 + trait |
| 空值 | `null` | `Option::None` |
| 异常 | `try-catch-throw` | `Result<T, E>` + `?` |
| 包/模块 | `package` + `import` | `mod` + `use` |
| 泛型 | `<T>` | `<T>` |
| Lambda | `(x) -> x + 1` | `\|x\| x + 1` |
| 注解 | `@Override` | `#[attribute]` |
| 构造函数 | `new User()` | `User::new()` (约定) |
| Getter | `getX()` | 直接访问或 `x()` |
| 静态方法 | `static method()` | `impl X { fn method() }` 无 self |
| 实例方法 | `method()` | `impl X { fn method(&self) }` |

---

## 🧠 记忆技巧

### 有符号 vs 无符号

```
i = integer = 有符号 = 可正可负（有 sign 正负号）
u = unsigned = 无符号 = 只能 ≥ 0（没有负号）
```

### 引用符号

```
&   借你看看（不可变引用）
&mut 借你改改（可变引用）
*   还回去/解开看（解引用）
```

### 所有权三原则

```
1. 每个值有且只有一个 Owner
2. Owner 离开作用域，值被 Drop
3. 同一时间：多个 &T 或 一个 &mut T
```

---

## 🚀 常用命令

```bash
cargo new project_name    # 创建新项目
cargo build               # 编译
cargo run                 # 运行
cargo test                # 测试
cargo fmt                 # 格式化
cargo clippy              # 代码检查
cargo doc --open          # 生成文档
cargo add package_name    # 添加依赖
```

---

## 📚 学习资源

- [Rust 官方文档](https://doc.rust-lang.org/)
- [Rust Book 中文](https://kaisery.github.io/trpl-zh-cn/)
- [Rust by Example](https://doc.rust-lang.org/rust-by-example/)
- [Rust 标准库](https://doc.rust-lang.org/std/)
- [Crates.io](https://crates.io/) - Rust 包仓库
